/**
 * Global switch for YooKassa / subscription checkout UI and client calls.
 * Set to true after INN (or other required registration) and acquiring are ready.
 */
export const PAYMENTS_ENABLED = false;
