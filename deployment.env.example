# Copy to deployment.env (gitignored).
# DEPLOY_HOST=YOUR_VM_PUBLIC_IP

# Ubuntu / Debian cloud images usually forbid SSH as root — use the default login user:
DEPLOY_USER=ubuntu
DEPLOY_USE_SUDO=true

DEPLOY_PATH=/var/www/china-cloud/dist
# DEPLOY_SSH_PORT=22
